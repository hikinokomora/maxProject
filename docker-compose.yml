version: '3.8'

services:
  chatbot:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    volumes:
      - ./server/config:/app/server/config
    restart: unless-stopped
