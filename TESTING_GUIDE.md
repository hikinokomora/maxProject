# 🧪 Руководство по тестированию

## ✅ Текущий статус

Приложение **запущено и готово к тестированию!**

### Что работает:
- ✅ Backend сервер на порту 5000
- ✅ React клиент на порту 3000
- ✅ SQLite база данных (dev.db)
- ✅ MAX Bot интеграция
- ✅ Автоматическое создание админа

### Доступы:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **Admin**: admin@university.edu / changeme123

---

## 🌐 Тестирование в браузере

### Откройте приложение
```
http://localhost:3000
```

### Что можно протестировать:

#### 1. Чат-бот
- Напишите "привет" или "помощь"
- Бот ответит с доступными командами

#### 2. Расписание
- Напишите "расписание" или "когда пары"
- Бот покажет расписание на неделю

#### 3. Мероприятия
- Напишите "мероприятия" или "что нового"
- Бот покажет предстоящие события

#### 4. Подать заявление
- Напишите "заявление" или "справка"
- Следуйте инструкциям бота
- Выберите тип заявления (Справка, Академический отпуск, и т.д.)
- Заполните форму

#### 5. Получить справку
- Напишите "справка об обучении"
- Бот запросит необходимые данные

---

## 📱 Тестирование в MAX Messenger

### Шаг 1: Настройка ngrok

Для тестирования мини-приложения в MAX нужен публичный HTTPS URL:

```powershell
# Установите ngrok (если еще не установлен)
# Скачайте с https://ngrok.com/download

# Запустите ngrok
ngrok http 3000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:3000
```

### Шаг 2: Обновите конфигурацию

Скопируйте HTTPS URL из ngrok и обновите `server/.env`:

```env
MINI_APP_URL=https://abc123.ngrok.io
```

Перезапустите сервер:
```powershell
# Ctrl+C в терминале сервера
cd C:\Users\marti\Documents\maxProject\server
npm start
```

### Шаг 3: Откройте бот в MAX

1. Найдите вашего бота в MAX messenger
2. Напишите `/start`
3. Нажмите кнопку **"🚀 Открыть приложение"**
4. Мини-приложение откроется внутри MAX!

### Что протестировать в MAX:
- ✅ Автоматическая авторизация (без ввода пароля)
- ✅ Персонализированное приветствие с вашим именем
- ✅ Все функции чат-бота
- ✅ Подача заявлений

---

## 🔧 API тестирование (Postman/curl)

### Регистрация нового пользователя
```powershell
curl -X POST http://localhost:5000/api/auth/register `
  -H "Content-Type: application/json" `
  -d '{
    "email": "student@uni.edu",
    "password": "password123",
    "name": "Иван Иванов"
  }'
```

### Вход
```powershell
curl -X POST http://localhost:5000/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{
    "email": "student@uni.edu",
    "password": "password123"
  }'
```

Ответ:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": 2,
    "email": "student@uni.edu",
    "name": "Иван Иванов",
    "role": "STUDENT"
  }
}
```

### Создание заявления (требует токен)
```powershell
$token = "ваш_токен_из_login"

curl -X POST http://localhost:5000/api/applications `
  -H "Authorization: Bearer $token" `
  -H "Content-Type: application/json" `
  -d '{
    "type": "certificate",
    "typeName": "Справка об обучении",
    "studentName": "Иван Иванов",
    "studentId": "12345",
    "department": "Информатика",
    "email": "student@uni.edu",
    "description": "Нужна для работы"
  }'
```

### Просмотр всех заявлений (только для TEACHER/STAFF/ADMIN)
```powershell
curl -X GET http://localhost:5000/api/applications `
  -H "Authorization: Bearer $token"
```

---

## 🐛 Возможные проблемы

### Порт уже занят
```powershell
# Найдите процесс
netstat -ano | findstr :5000

# Остановите его
taskkill /F /PID [номер_процесса]
```

### База данных не создается
```powershell
cd server
npx prisma generate
npx prisma db push
```

### Клиент не подключается к серверу
Проверьте, что в `client/package.json` есть:
```json
"proxy": "http://localhost:5000"
```

### Ошибки CORS
Убедитесь, что сервер запущен и в `server/index.js` настроен CORS:
```javascript
app.use(cors());
```

---

## 📊 Что проверить

### Функциональность
- [ ] Чат-бот отвечает на команды
- [ ] Расписание показывается корректно
- [ ] Мероприятия отображаются
- [ ] Можно подать заявление
- [ ] Заявления сохраняются в БД
- [ ] JWT токены работают
- [ ] Регистрация/вход работают

### UI/UX
- [ ] Интерфейс отзывчивый
- [ ] Сообщения отображаются корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Анимации плавные

### MAX Integration
- [ ] Мини-апп открывается в MAX
- [ ] Автоаутентификация работает
- [ ] Данные отправляются боту
- [ ] Кнопки работают

---

## 🎯 Следующие шаги

После успешного тестирования:

1. **Миграция на PostgreSQL** (для продакшена)
   - См. [POSTGRES_QUICKSTART.md](./POSTGRES_QUICKSTART.md)

2. **Интеграция Tuna SDK** (когда будет доступен)
   - См. [TUNA_INTEGRATION.md](./TUNA_INTEGRATION.md)

3. **UI улучшения**
   - Добавить формы входа/регистрации
   - Личный кабинет студента
   - Панель преподавателя

4. **Деплой**
   - Backend: Heroku, Railway, или VPS
   - Frontend: Vercel, Netlify
   - База: PostgreSQL на Railway или Supabase

---

## 📞 Нужна помощь?

- **Логи сервера**: смотрите в терминал где запущен `npm start` (server)
- **Логи клиента**: откройте DevTools в браузере (F12) → Console
- **База данных**: используйте `npx prisma studio` для просмотра

---

**Приятного тестирования!** 🚀
