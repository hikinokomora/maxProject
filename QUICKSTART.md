# Быстрый старт

## За 5 минут до первого запуска

### Вариант 1: Локальная разработка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/hikinokomora/maxProject.git
cd maxProject

# 2. Установите зависимости для сервера
cd server
npm install

# 3. В новом терминале установите зависимости для клиента
cd ../client
npm install

# 4. Запустите сервер (в первом терминале)
cd ../server
npm run dev

# 5. Запустите клиент (во втором терминале)
cd ../client
npm start

# 6. Откройте браузер
# Приложение автоматически откроется на http://localhost:3000
```

### Вариант 2: Docker (самый простой)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/hikinokomora/maxProject.git
cd maxProject

# 2. Соберите и запустите
docker build -t max-chatbot .
docker run -p 5000:5000 max-chatbot

# 3. Откройте браузер
# Перейдите на http://localhost:5000
```

## Первое использование

После запуска приложения:

1. **Поздоровайтесь с ботом**
   - Напишите "Привет" или "Здравствуйте"
   - Бот ответит приветствием и покажет доступные команды

2. **Попробуйте основные функции**
   
   **Расписание:**
   - Напишите "расписание"
   - Бот попросит уточнить группу и день

   **Мероприятия:**
   - Напишите "мероприятия"
   - Бот покажет список предстоящих событий

   **Заявления:**
   - Напишите "заявление"
   - Бот покажет доступные типы заявлений

3. **Получите помощь**
   - Напишите "помощь"
   - Бот покажет все доступные команды

## Демонстрационный режим

Для быстрого теста через API:

```bash
# Убедитесь, что сервер запущен
cd server
npm start

# В другом терминале запустите демо
cd ..
chmod +x demo.sh
./demo.sh
```

## Настройка под свой вуз

1. **Отредактируйте конфигурацию**
   ```bash
   nano server/config/university.json
   ```
   
2. **Измените название университета**
   ```json
   {
     "universityName": "Ваш Университет",
     "universityShortName": "ВУ"
   }
   ```

3. **Добавьте свои типы заявлений**
   ```json
   "applicationTypes": [
     {
       "id": "your_type",
       "name": "Ваше заявление",
       "description": "Описание"
     }
   ]
   ```

4. **Перезапустите сервер**
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   npm start
   ```

## Устранение проблем

### Порт 3000 или 5000 занят

```bash
# Найдите процесс, использующий порт
lsof -i :3000
lsof -i :5000

# Остановите процесс
kill -9 <PID>
```

### Ошибки при установке зависимостей

```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и установите заново
rm -rf node_modules
npm install
```

### Проблемы с Docker

```bash
# Убедитесь, что Docker запущен
docker --version

# Очистите старые образы
docker system prune -a

# Пересоберите образ
docker build --no-cache -t max-chatbot .
```

## Следующие шаги

После успешного запуска:

1. 📖 Прочитайте [SETUP.md](SETUP.md) для детальной настройки
2. 💡 Изучите [EXAMPLES.md](EXAMPLES.md) для добавления функций
3. 🏗️ Ознакомьтесь с [ARCHITECTURE.md](ARCHITECTURE.md) для понимания структуры
4. 🔧 Адаптируйте под ваши нужды!

## Полезные команды

```bash
# Проверка работы сервера
curl http://localhost:5000/health

# Тест API
curl -X POST http://localhost:5000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "привет"}'

# Просмотр логов Docker
docker logs <container-id>

# Остановка всех контейнеров
docker stop $(docker ps -q)
```

## Получение помощи

- 📝 Создайте Issue в GitHub
- 💬 Опишите вашу проблему
- 🔍 Приложите логи ошибок

Удачи в запуске! 🚀
