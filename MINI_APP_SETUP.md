# Настройка мини-приложения MAX

## Что такое мини-приложение?

Мини-приложение (Mini App) в MAX - это веб-приложение, которое открывается внутри мессенджера. Пользователи могут взаимодействовать с полным интерфейсом вашего приложения, не покидая MAX.

## Как это работает

1. В боте есть кнопка **"🚀 Открыть приложение"**
2. При нажатии открывается ваше React-приложение в браузере
3. Пользователь видит полный интерфейс с чатом, расписанием и т.д.

## Настройка для локального тестирования

### Вариант 1: Использование localhost (только для тестирования на том же компьютере)

1. В `.env` укажите:
   ```bash
   MINI_APP_URL=http://localhost:3000
   ```

2. Запустите приложение:
   ```bash
   docker compose up -d
   ```

3. **Ограничение**: Ссылка будет работать только на вашем компьютере

### Вариант 2: Использование ngrok (рекомендуется для тестирования)

[ngrok](https://ngrok.com) создаёт публичный HTTPS URL для вашего локального приложения.

1. **Установите ngrok**:
   - Скачайте с https://ngrok.com/download
   - Или через chocolatey: `choco install ngrok`

2. **Запустите ngrok**:
   ```bash
   ngrok http 3000
   ```

3. **Скопируйте HTTPS URL** (например, `https://abc123.ngrok.io`)

4. **Обновите `.env`**:
   ```bash
   MINI_APP_URL=https://abc123.ngrok.io
   ```

5. **Перезапустите бота**:
   ```bash
   docker compose restart chatbot
   ```

Теперь кнопка "Открыть приложение" будет работать на любом устройстве!

## Настройка для продакшена

### Развертывание на облачном хостинге

1. **Разверните приложение** на одном из сервисов:
   - [Heroku](https://heroku.com)
   - [Railway](https://railway.app)
   - [Render](https://render.com)
   - [Vercel](https://vercel.com) (для статики)
   - [DigitalOcean](https://digitalocean.com)

2. **Получите публичный URL** (например, `https://your-app.herokuapp.com`)

3. **Обновите `.env`**:
   ```bash
   MINI_APP_URL=https://your-app.herokuapp.com
   ```

4. **Пересоберите и перезапустите**:
   ```bash
   docker compose build chatbot
   docker compose up -d
   ```

## Использование

После настройки пользователи увидят в боте:

```
👋 Добро пожаловать в чат-бот университета!

[📅 Расписание] [🎯 Мероприятия] [📝 Заявления]
[🚀 Открыть приложение]
[❓ Помощь]
```

При нажатии на **"🚀 Открыть приложение"** откроется полный веб-интерфейс.

## Структура проекта

```
maxProject/
├── client/          # React-приложение (мини-апп интерфейс)
│   ├── src/
│   │   ├── App.js
│   │   └── components/
│   │       └── ChatBot.jsx
│   └── public/
├── server/          # Node.js сервер + бот
│   ├── index.js
│   └── services/
│       └── maxBotService.js
└── .env            # MINI_APP_URL здесь
```

## Требования MAX для мини-приложений

- ✅ HTTPS обязателен для продакшена (ngrok предоставляет автоматически)
- ✅ Адаптивный дизайн (ваше React-приложение уже адаптивное)
- ✅ Работа в iframe (React поддерживает по умолчанию)

## Дополнительные возможности

### Интеграция с MAX SDK (опционально)

Можно добавить MAX Web SDK для глубокой интеграции:

```javascript
// В client/src/index.js
import { MaxApp } from '@maxhub/max-app-sdk';

const maxApp = new MaxApp();

// Получить данные пользователя
maxApp.getUserData().then(user => {
  console.log('User:', user);
});

// Закрыть мини-приложение
maxApp.close();
```

### Персонализация

Передавайте данные пользователя через URL параметры:

```javascript
// В maxBotService.js
buildMainKeyboardWithApp() {
  const userId = ctx.user?.user_id;
  const appUrl = `${process.env.MINI_APP_URL}?user=${userId}`;
  
  // ...
}
```

## Отладка

Проверьте логи бота:
```bash
docker compose logs -f chatbot
```

Проверьте, что приложение доступно:
```bash
curl http://localhost:3000
```

## Полезные ссылки

- [MAX Bot API Documentation](https://dev.max.ru/docs/chatbots/)
- [ngrok Documentation](https://ngrok.com/docs)
- [React Deployment Guide](https://create-react-app.dev/docs/deployment/)
