# MAX Messenger Chatbot - PostgreSQL & JWT Edition

Универсальный чат-бот для мессенджера MAX для цифровизации вузовских процессов с полноценной аутентификацией.

## ✨ Возможности

- 🔐 **JWT Аутентификация** - безопасная система входа и регистрации
- 📝 **Подача заявлений** - автоматизированная обработка студенческих заявлений с привязкой к пользователям
- 👥 **Управление ролями** - STUDENT, TEACHER, STAFF, ADMIN с разными правами доступа
- 📊 **Панель для преподавателей** - просмотр всех заявлений с реальными именами студентов
- 📅 **Расписание** - быстрый доступ к расписанию занятий и экзаменов
- 🎉 **Мероприятия** - информация о предстоящих университетских событиях
- 🚀 **Мини-приложение** - полный веб-интерфейс прямо в мессенджере
- 🔧 **Легкая настройка** - простая адаптация под разные вузы через конфигурационные файлы
- 🐳 **Docker-ready** - быстрый запуск через Docker

## 🛠 Технологии

- **Frontend**: React.js с современным UI/UX
- **Backend**: Node.js + Express
- **Database**: PostgreSQL + Prisma ORM
- **Authentication**: JWT + bcrypt
- **MAX Bot**: Официальный SDK `@maxhub/max-bot-api`
- **Контейнеризация**: Docker

## 🚀 Быстрый старт

### Вариант 1: Локальная установка (рекомендуется)

**1. Установите PostgreSQL**

См. подробную инструкцию в [`POSTGRES_QUICKSTART.md`](./POSTGRES_QUICKSTART.md)

**2. Создайте базу данных**

```bash
psql -U postgres
CREATE DATABASE maxchatbot;
\q
```

**3. Настройте `server/.env`**

```env
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/maxchatbot

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# MAX Bot
BOT_TOKEN=ваш_токен_от_MAX

# Admin (создается автоматически)
ADMIN_EMAIL=admin@university.edu
ADMIN_NAME=Administrator
ADMIN_PASSWORD=changeme123

# Server
PORT=5000
NODE_ENV=development

# Mini App
MINI_APP_URL=http://localhost:3000
```

**4. Запустите миграцию и сервер**

```bash
cd server
npm install
npx prisma generate
npx prisma db push
npm start
```

**5. Запустите клиент (в другом терминале)**

```bash
cd client
npm install
npm start
```

### Вариант 2: Docker

```bash
docker compose up --build -d
docker compose logs -f
```

## 📚 Документация

- **[POSTGRES_QUICKSTART.md](./POSTGRES_QUICKSTART.md)** ⚡ Быстрая настройка PostgreSQL
- **[MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)** 📖 Полная документация по API и безопасности
- **[SETUP.md](./SETUP.md)** - Детальная настройка проекта
- **[FEATURES.md](./FEATURES.md)** - Описание функциональности
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - Архитектура системы

## 🔑 API Endpoints

### Аутентификация

```bash
# Регистрация
POST /api/auth/register
Body: { email, password, name, role? }

# Вход
POST /api/auth/login
Body: { email, password }

# Профиль
GET /api/auth/me
Headers: Authorization: Bearer <token>
```

### Заявления

```bash
# Создать заявление (требует аутентификацию)
POST /api/applications
Headers: Authorization: Bearer <token>

# Список всех заявлений (TEACHER/STAFF/ADMIN)
GET /api/applications
Headers: Authorization: Bearer <token>

# Обновить статус (TEACHER/STAFF/ADMIN)
PATCH /api/applications/:id/status
Headers: Authorization: Bearer <token>
Body: { status }
```

Подробнее см. [`MIGRATION_GUIDE.md`](./MIGRATION_GUIDE.md#📋-изменения-в-api)

## 👥 Роли пользователей

| Роль | Описание | Права |
|------|----------|-------|
| **STUDENT** | Студент | Создание и просмотр своих заявлений |
| **TEACHER** | Преподаватель | + Просмотр всех заявлений, обновление статусов |
| **STAFF** | Сотрудник | То же что TEACHER |
| **ADMIN** | Администратор | + Полный доступ, управление пользователями |

## 🔐 Безопасность

- ✅ JWT токены с configurable expiration
- ✅ Bcrypt хеширование паролей (10 rounds)
- ✅ Rate limiting на API endpoints (100 req/15min)
- ✅ Валидация входных данных
- ✅ CORS настройка
- ⚠️ **В production обязательно**:
  - Смените `JWT_SECRET` на случайный ключ
  - Смените пароль admin после первого входа
  - Используйте HTTPS
  - Настройте CORS для вашего домена
  - Используйте переменные окружения для секретов

## 🧪 Тестирование

### PowerShell

```powershell
# Вход как admin
$body = @{
    email = "admin@university.edu"
    password = "changeme123"
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:5000/api/auth/login" `
    -Method POST -Body $body -ContentType "application/json"

$token = $response.token

# Просмотр заявлений
Invoke-RestMethod -Uri "http://localhost:5000/api/applications" `
    -Headers @{ Authorization = "Bearer $token" }
```

### Bash

```bash
# Вход как admin
TOKEN=$(curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@university.edu","password":"changeme123"}' \
  | jq -r '.token')

# Просмотр заявлений
curl http://localhost:5000/api/applications \
  -H "Authorization: Bearer $TOKEN"
```

Больше примеров в [`MIGRATION_GUIDE.md`](./MIGRATION_GUIDE.md#🧪-тестирование)

## 🤖 Интеграция с MAX Bot

Бот автоматически:
1. Создает пользователя при первой подаче заявления через мессенджер
2. Привязывает MAX user ID к профилю
3. Использует реальное имя студента из профиля
4. Преподаватели видят настоящие имена, не ники

## 🐛 Troubleshooting

### Порт уже занят
```powershell
# Измените PORT в .env или остановите процесс на 5000
netstat -ano | findstr :5000
taskkill /PID <PID> /F
```

### PostgreSQL не запускается
```powershell
# Проверьте статус
pg_ctl status

# Запустите вручную
pg_ctl start -D "C:\Program Files\PostgreSQL\16\data"
```

### Prisma ошибки
```bash
# Пересоздайте клиент
npx prisma generate
npx prisma db push --force-reset  # ⚠️ Удалит все данные!
```

## 📦 Структура проекта

```
maxProject/
├── client/              # React frontend
│   ├── src/
│   │   ├── components/  # ChatBot компонент
│   │   └── services/    # API интеграция
│   └── package.json
├── server/              # Node.js backend
│   ├── config/          # Конфигурация университета
│   ├── middleware/      # JWT auth middleware
│   ├── prisma/          # Database schema
│   ├── routes/          # API endpoints
│   ├── services/        # Бизнес-логика
│   └── index.js         # Entry point
├── POSTGRES_QUICKSTART.md  # ⚡ Быстрый старт
├── MIGRATION_GUIDE.md      # 📖 Полная документация
└── docker-compose.yml      # Docker конфигурация
```

## 📝 Следующие шаги

1. ✅ Установите PostgreSQL
2. ✅ Настройте `.env`
3. ✅ Запустите миграции
4. ✅ Создайте тестовых пользователей
5. ⏭️ Настройте фронтенд для работы с JWT
6. ⏭️ Протестируйте бота в MAX мессенджере
7. ⏭️ Адаптируйте `server/config/university.json` под ваш вуз
8. ⏭️ Разверните в production с HTTPS

## 📞 Поддержка

- Документация: См. файлы `*.md` в корне проекта
- Issues: Создайте issue в репозитории
- MAX SDK: https://dev.max.ru/docs

## 📄 Лицензия

MIT
