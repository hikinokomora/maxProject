# MAX Messenger Bot Integration

Бот подключается напрямую к мессенджеру MAX через официальный SDK `@maxhub/max-bot-api`.

## Документация

- [Официальная документация MAX Bot API](https://dev.max.ru/docs/chatbots/bots-coding/library/js)
- [GitHub SDK](https://github.com/max-messenger/max-bot-api-client-ts)

## Быстрый старт

### 1. Зарегистрируйте бота

1. Зайдите на [dev.max.ru](https://dev.max.ru)
2. Следуйте инструкции: [Подготовка и управление](https://dev.max.ru/docs/chatbots/bots-nocode/manage)
3. Скопируйте токен из настроек бота (карточка бота)

### 2. Настройте окружение

Создайте файл `server/.env`:
```env
BOT_TOKEN=ваш_токен_от_MAX
PORT=5000
```

Или в корне проекта `.env` для Docker:
```env
BOT_TOKEN=ваш_токен_от_MAX
```

### 3. Запустите бота

#### Локально (для разработки):
```bash
cd server
npm install
npm start
```

#### В Docker (production):
```powershell
docker compose down
docker compose build --no-cache
docker compose up -d
docker compose logs -f chatbot
```

## Как работает

### Архитектура
- **Официальный SDK**: Использует `@maxhub/max-bot-api` 
- **Long Polling**: SDK автоматически опрашивает MAX API
- **Event Handlers**: Обработчики событий для разных типов обновлений
- **Inline Keyboard**: Кнопки-подсказки для быстрых ответов

### Обработчики событий

```javascript
bot.command('start', ...)      // Команда /start
bot.on('message_created', ...) // Новое сообщение
bot.on('message_callback', ...) // Нажатие на кнопку
bot.on('bot_started', ...)      // Пользователь начал диалог
bot.on('bot_added', ...)        // Бот добавлен в чат
```

## Логи

### При успешном запуске:
```
🚀 MAX Chatbot Server running on port 5000
🔐 BOT_TOKEN: abc123...xyz789
[MAX Bot] Bot instance created
[MAX Bot] Starting bot...
[MAX Bot] ✅ Bot started successfully and listening for updates
```

### При получении сообщения:
```
[MAX Bot] Message from 12345: привет
[CHAT] Incoming payload: { message: 'привет' }
[CHAT] Responding with: { text: '...', suggestions: [...] }
```

## Возможности бота

### ✅ Текстовые сообщения
- Обработка текста через `chatService.js`
- Умное распознавание намерений (расписание, мероприятия, заявления)

### ✅ Inline-клавиатура
- Кнопки-подсказки для быстрых ответов
- Автоматическая группировка (до 3 кнопок в ряд)
- Callback-обработка нажатий

### ✅ Markdown форматирование
- Жирный текст: `**текст**`
- Курсив: `_текст_`
- Ссылки: `[текст](url)`

### ✅ Команды
- `/start` — приветствие и главное меню
- Текстовые команды из конфига

### ✅ События и мероприятия
- Автоматическое форматирование списков
- Emoji для наглядности

## Настройка

### Изменение логики ответов
Редактируйте `server/services/chatService.js`:
```javascript
processMessage(message) {
  // Ваша логика обработки
  return {
    text: 'Ответ пользователю',
    suggestions: ['Кнопка 1', 'Кнопка 2']
  };
}
```

### Добавление новых обработчиков
Редактируйте `server/services/maxBotService.js`:
```javascript
this.bot.on('message_edited', async (ctx) => {
  // Обработка редактирования сообщений
});
```

### Конфигурация университета
Редактируйте `server/config/university.json`:
```json
{
  "universityName": "Ваш университет",
  "welcomeMessage": "Приветствие",
  "chatCommands": [...],
  "applicationTypes": [...]
}
```

## Отладка

### Проблема: Бот не запускается
✅ Проверьте токен в `.env`
✅ Убедитесь, что токен актуален (проверьте в настройках бота на dev.max.ru)
✅ Проверьте логи: `docker compose logs -f chatbot`

### Проблема: Бот не отвечает
✅ Убедитесь, что бот запустился (`✅ Bot started successfully`)
✅ Проверьте, есть ли логи при отправке сообщения
✅ Убедитесь, что бот активирован в настройках MAX

### Проблема: Кнопки не работают
✅ Проверьте обработчик `message_callback`
✅ Убедитесь, что payload кнопки передаётся корректно
✅ Посмотрите логи callback-событий

## Тестирование

1. Найдите своего бота в MAX messenger
2. Отправьте `/start`
3. Протестируйте команды:
   - "привет" → приветствие + кнопки
   - "расписание" → запрос деталей
   - "мероприятия" → список событий
   - "заявление" → типы заявлений
   - "помощь" → список команд

## Production Checklist

- [ ] Токен бота в `.env` (не в коде!)
- [ ] `.env` в `.gitignore`
- [ ] Проверены все обработчики событий
- [ ] Настроен graceful shutdown
- [ ] Логирование всех критичных операций
- [ ] Обработка ошибок с fallback-сообщениями
- [ ] Тестирование в реальном MAX messenger

## Дополнительно

### Webhooks вместо Long Polling
Если нужна более высокая производительность, можно переключиться на webhooks.
См. документацию: [MAX Webhooks](https://dev.max.ru/docs/chatbots/bots-coding/webhooks)

### Расширенные возможности
- Загрузка файлов (изображения, документы)
- Геолокация
- Контакты
- Стикеры
- Mini Apps

См. [полную документацию SDK](https://dev.max.ru/docs/chatbots/bots-coding/library/js)

## Поддержка

- [Официальная документация](https://dev.max.ru)
- [GitHub Issues](https://github.com/max-messenger/max-bot-api-client-ts/issues)
- [Раздел помощи](https://dev.max.ru/help)
