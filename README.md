# MAX Messenger Chatbot MVP

Универсальный чат-бот для мессенджера MAX для цифровизации вузовских процессов.

## 🎯 Возможности

### Для студентов
- 📝 **Подача заявлений** - автоматизированная обработка через бот или мини-апп
- 📅 **Расписание** - быстрый доступ к расписанию занятий и экзаменов
- 🎉 **Мероприятия** - информация о предстоящих университетских событиях
- 🌐 **Мини-приложение** - полный веб-интерфейс с автоматической авторизацией
- � **Личный кабинет** - управление заявлениями и профилем

### Для преподавателей и сотрудников
- 👥 **Панель администратора** - просмотр всех заявлений с реальными именами
- ✅ **Управление статусами** - принятие/отклонение заявок
- �‍🎓 **Профили студентов** - полная информация о пользователях

## 🚀 Технологии

### Frontend
- **React 18.2.0** - современный UI с хуками
- **Tuna SDK (@maxhub/tuna)** - официальный SDK для мини-приложений MAX
- **Axios** - HTTP клиент
- **Адаптивный UI** - работает в браузере и внутри MAX

### Backend
- **Node.js + Express** - REST API сервер
- **PostgreSQL + Prisma ORM** - надежное хранение данных
- **JWT Authentication** - безопасная аутентификация
- **bcrypt** - хеширование паролей
- **MAX Bot SDK** - интеграция с мессенджером

### Архитектура
- **Универсальность**: одно приложение работает как веб-сайт и мини-апп
- **Автоматическая аутентификация**: пользователи MAX входят без регистрации
- **Роли и права**: STUDENT, TEACHER, STAFF, ADMIN
- **Контейнеризация**: Docker Compose для быстрого развертывания

## 🚀 Быстрый старт

### Вариант 1: Запуск в Docker (Рекомендуется)

**1. Убедитесь, что Docker Desktop запущен**

**2. Запустите проект одной командой:**
```powershell
# Windows PowerShell
.\start-docker.ps1

# Или вручную
docker-compose up --build -d
```

**3. Откройте приложение:**
- 🌐 **Веб-приложение**: http://localhost:3000
- 🔧 **API**: http://localhost:5000

**4. Войдите в систему:**
- Email: `admin@university.edu`
- Password: `admin123`

**Управление контейнерами:**
```powershell
# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Или используйте скрипт
.\stop-docker.ps1
```

---

### Вариант 2: Локальная разработка (без Docker)

**1. Подготовка базы данных PostgreSQL**
```powershell
# Создайте базу данных
psql -U postgres
CREATE DATABASE maxchatbot;
\q
```

**2. Настройка сервера**

Создайте `server/.env`:
```env
# MAX Bot
BOT_TOKEN=ваш_токен_от_MAX
MINI_APP_URL=https://your-ngrok-url.ngrok.io

# PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/maxchatbot

# JWT
JWT_SECRET=your-super-secret-key-change-this
JWT_EXPIRES_IN=7d

# Admin (будет создан автоматически)
ADMIN_EMAIL=admin@university.edu
ADMIN_NAME=Admin
ADMIN_PASSWORD=admin123

# Server
PORT=5000
NODE_ENV=development
```

**3. Установка зависимостей**
```powershell
# Backend
cd server
npm install
npx prisma generate
npx prisma db push

# Frontend
cd ../client
npm install
```

**4. Запуск**
```powershell
# Terminal 1: Backend
cd server
npm start

# Terminal 2: Frontend
cd client
npm start
```

**5. Тестирование в браузере**
- Откройте http://localhost:3000
- Приложение работает как обычный сайт
- Доступна регистрация/вход через email

**6. (Опционально) Тестирование в MAX**
```powershell
# Установите ngrok
ngrok http 3000

# Обновите MINI_APP_URL в server/.env
# Откройте бот в MAX → кнопка "Открыть приложение"
```

📚 **Подробные инструкции:**
- [TUNA_QUICKSTART.md](./TUNA_QUICKSTART.md) - **НАЧНИТЕ ОТСЮДА** (5 минут)
- [TUNA_INTEGRATION.md](./TUNA_INTEGRATION.md) - полное руководство
- [POSTGRES_QUICKSTART.md](./POSTGRES_QUICKSTART.md) - настройка PostgreSQL
- [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) - полная миграция

### Вариант 2: Docker (упрощенный, без PostgreSQL)

```powershell
docker compose up --build -d
docker compose logs -f chatbot
```

## 🎯 Основные фичи

### 🔐 Аутентификация

**Два способа входа:**

1. **Через MAX (автоматически):**
   - Пользователь открывает мини-апп из бота
   - Tuna SDK получает данные из MAX (имя, ID)
   - Backend автоматически создает/находит пользователя
   - Генерируется JWT токен
   - Пользователь залогинен без ввода пароля

2. **Через email/password (вручную):**
   - Пользователь открывает сайт в браузере
   - Регистрация через `POST /api/auth/register`
   - Вход через `POST /api/auth/login`
   - Получение JWT токена

**Роли:**
- `STUDENT` - по умолчанию
- `TEACHER` - просмотр всех заявлений
- `STAFF` - управление заявлениями
- `ADMIN` - полный доступ (создается автоматически из .env)

### 📱 Мини-приложение MAX (Tuna SDK)

**Возможности:**
```javascript
// Получить данные пользователя
const user = await tunaService.getUserInfo();
// { id: 123, firstName: "Иван", lastName: "Иванов" }

// Показать уведомление
await tunaService.showAlert('Заявление отправлено!');

// Подтверждение
const confirmed = await tunaService.showConfirm('Удалить?');

// Отправить данные боту
await tunaService.sendDataToBot({ action: 'created', id: 123 });

// Главная кнопка внизу экрана
tunaService.setMainButton('Отправить', handleSubmit);

// Адаптация под тему MAX
const theme = tunaService.getThemeParams();
// { bg_color: '#ffffff', text_color: '#000000', ... }
```

**Адаптивность:**
- В MAX - автоаутентификация, нативный UI, отправка данных боту
- В браузере - обычный веб-сайт с формами входа

### 📝 API Endpoints

#### Аутентификация
```
POST /api/auth/register      - Регистрация (email, password, name)
POST /api/auth/login         - Вход (email, password)
GET  /api/auth/me            - Текущий пользователь (требует JWT)
POST /api/auth/tuna-login    - Автовход через Tuna (maxUserId, email, name)
POST /api/auth/link-max      - Привязать MAX ID к аккаунту
```

#### Заявления
```
POST /api/applications       - Создать заявление (требует JWT)
GET  /api/applications       - Все заявления (TEACHER/STAFF/ADMIN)
GET  /api/applications/:id   - Конкретное заявление
PATCH /api/applications/:id/status - Обновить статус (TEACHER+)
```

См. [API_EXAMPLES.md](./API_EXAMPLES.md) для примеров запросов.

## 📂 Структура проекта

```
maxProject/
├── client/                          # React Frontend
│   ├── src/
│   │   ├── components/
│   │   │   └── ChatBot.jsx         # Главный компонент чата
│   │   ├── services/
│   │   │   ├── tunaService.js      # 🆕 Обертка Tuna SDK
│   │   │   ├── authService.js      # 🆕 JWT + Tuna аутентификация
│   │   │   └── chatService.js      # API клиент
│   │   ├── App.js                  # 🆕 Tuna инициализация
│   │   └── index.js
│   └── package.json                # 🆕 + @maxhub/tuna
│
├── server/                          # Express Backend
│   ├── routes/
│   │   ├── auth.js                 # 🆕 JWT + Tuna endpoints
│   │   ├── applications.js         # 🔐 Защищенные JWT
│   │   ├── chat.js
│   │   ├── events.js
│   │   └── schedule.js
│   ├── services/
│   │   ├── authService.js          # 🆕 Регистрация, login, JWT
│   │   ├── seedService.js          # 🆕 Автосоздание админа
│   │   ├── applicationsService.js  # ✨ Привязка к пользователям
│   │   ├── maxBotService.js        # ✨ Авто-создание из бота
│   │   ├── chatService.js
│   │   ├── eventsService.js
│   │   └── scheduleService.js
│   ├── middleware/
│   │   └── auth.js                 # 🆕 JWT middleware
│   ├── prisma/
│   │   └── schema.prisma           # 🆕 PostgreSQL схема
│   ├── config/
│   │   └── university.json
│   ├── .env                        # 🆕 DATABASE_URL, JWT_SECRET
│   ├── index.js                    # ✨ + seedAdmin()
│   └── package.json                # 🆕 + bcrypt, jsonwebtoken, pg
│
├── TUNA_QUICKSTART.md              # 🆕 Быстрый старт Tuna (5 мин)
├── TUNA_INTEGRATION.md             # 🆕 Полное руководство Tuna
├── TUNA_SUMMARY.md                 # 🆕 Краткий обзор
├── MIGRATION_GUIDE.md              # 🆕 PostgreSQL + JWT миграция
├── POSTGRES_QUICKSTART.md          # 🆕 Настройка PostgreSQL
├── API_EXAMPLES.md                 # 🆕 Примеры API запросов
├── README_NEW.md                   # 🆕 Обновленная документация
├── NEXT_STEPS.md                   # 🆕 План развития
├── docker-compose.yml
└── Dockerfile

🆕 - новые файлы
✨ - обновленные файлы
🔐 - добавлена JWT защита
```

## 🧪 Тестирование

### 1. Тест в браузере (без MAX)
```powershell
cd client
npm start
```
- Откройте http://localhost:3000
- Приложение работает в режиме веб-сайта
- Зарегистрируйтесь через email/password
- Проверьте создание заявления

### 2. Тест в MAX (мини-апп)
```powershell
# Запустите ngrok
ngrok http 3000

# Обновите server/.env
MINI_APP_URL=https://xxxx.ngrok.io

# Запустите сервер
cd server
npm start

# Откройте бот в MAX → "Открыть приложение"
```
- Приложение откроется внутри MAX
- Автоматический вход (без пароля)
- Персонализированное приветствие
- Попробуйте создать заявление
- Данные отправятся боту

### 3. Тест API (через Postman/curl)
```powershell
# Регистрация
curl -X POST http://localhost:5000/api/auth/register `
  -H "Content-Type: application/json" `
  -d '{"email":"test@uni.edu","password":"123456","name":"Test User"}'

# Вход
$response = curl -X POST http://localhost:5000/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{"email":"test@uni.edu","password":"123456"}' | ConvertFrom-Json

# Создание заявления
curl -X POST http://localhost:5000/api/applications `
  -H "Authorization: Bearer $($response.token)" `
  -H "Content-Type: application/json" `
  -d '{"type":"Справка","reason":"Нужна для работы"}'
```

См. [API_EXAMPLES.md](./API_EXAMPLES.md) для подробных примеров.

## 📚 Документация

### Быстрый старт
- **[TUNA_QUICKSTART.md](./TUNA_QUICKSTART.md)** - 🚀 **НАЧНИТЕ ОТСЮДА** (5 минут)
- **[POSTGRES_QUICKSTART.md](./POSTGRES_QUICKSTART.md)** - Настройка PostgreSQL за 3 шага

### Интеграции
- **[TUNA_INTEGRATION.md](./TUNA_INTEGRATION.md)** - Полное руководство по Tuna SDK (100+ примеров)
- **[TUNA_SUMMARY.md](./TUNA_SUMMARY.md)** - Краткий обзор возможностей Tuna

### Миграция и API
- **[MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)** - Миграция на PostgreSQL + JWT
- **[API_EXAMPLES.md](./API_EXAMPLES.md)** - Примеры запросов к API
- **[README_NEW.md](./README_NEW.md)** - Детальная документация

### Планы
- **[NEXT_STEPS.md](./NEXT_STEPS.md)** - Дальнейшее развитие проекта

### Архитектура
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** - Общая архитектура
- **[PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)** - Структура файлов
- **[FEATURES.md](./FEATURES.md)** - Описание возможностей

## 🎉 Что получилось

### Три в одном
1. **MAX Bot** - обработка команд, клавиатуры, меню
2. **Мини-приложение** - полный UI внутри MAX с автологином
3. **Веб-сайт** - доступен из любого браузера

### Технологический стек
- ✅ PostgreSQL + Prisma ORM
- ✅ JWT аутентификация (bcrypt)
- ✅ Tuna SDK (@maxhub/tuna)
- ✅ React 18 + Hooks
- ✅ Express REST API
- ✅ Роли и права (STUDENT/TEACHER/STAFF/ADMIN)
- ✅ Автосоздание пользователей из MAX
- ✅ Привязка заявлений к реальным пользователям

### Готовые фичи
- ✅ Регистрация/вход через email или MAX
- ✅ Подача заявлений (через бот или мини-апп)
- ✅ Панель преподавателя с реальными именами
- ✅ Управление статусами заявок
- ✅ Адаптация под тему MAX
- ✅ Отправка данных обратно боту
- ✅ Главная кнопка и нативные уведомления
- ✅ Расписание, мероприятия, чат
- ✅ Автоматическое создание админа

## ⚡ Требования

- Node.js 18+
- PostgreSQL 14+
- npm 8+
- ngrok (для тестирования мини-аппа в MAX)

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

## 📄 Лицензия

MIT

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте [TUNA_QUICKSTART.md](./TUNA_QUICKSTART.md)
2. Изучите [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md)
3. Примеры запросов в [API_EXAMPLES.md](./API_EXAMPLES.md)
4. Создайте issue в репозитории

---

**Готово к использованию!** 🚀

Откройте [TUNA_QUICKSTART.md](./TUNA_QUICKSTART.md) и начните за 5 минут.
```

## Настройка под свой вуз

1. Отредактируйте файл `server/config/university.json`
2. Укажите название вуза, доступные услуги и контактную информацию
3. При необходимости добавьте собственные обработчики в `server/services/`

## API Endpoints

- `POST /api/chat` - отправка сообщения боту
- `GET /api/schedule` - получение расписания
- `GET /api/events` - список мероприятий
- `POST /api/applications` - подача заявления

## Разработка

### Добавление новой функциональности

1. Добавьте новый обработчик в `server/services/chatService.js`
2. Обновите UI в `client/src/components/ChatBot.jsx`
3. При необходимости обновите конфигурацию

### Тестирование

```bash
# В директории server
npm test

# В директории client
npm test
```

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создавайте Issues в репозитории.