# Быстрый старт мини-приложения

## 📋 Что добавлено

✅ Кнопка **"🚀 Открыть приложение"** в главном меню бота
✅ Поддержка переменной окружения `MINI_APP_URL`
✅ Документация по настройке (`MINI_APP_SETUP.md`)

## 🚀 Как запустить

### 1. Для локального тестирования (ngrok рекомендуется)

```bash
# Установите ngrok (если ещё не установлен)
# https://ngrok.com/download

# Запустите ngrok в отдельном терминале
ngrok http 3000

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Обновите .env:
MINI_APP_URL=https://abc123.ngrok.io

# Запустите Docker
docker compose up -d
```

### 2. Для быстрого теста (только на вашем компьютере)

```bash
# .env уже настроен с localhost
MINI_APP_URL=http://localhost:3000

# Просто запустите
docker compose up -d
```

## 🎯 Как использовать

1. Откройте бота в MAX мессенджере
2. Отправьте `/start`
3. Вы увидите новое меню:

```
👋 Добро пожаловать в чат-бот университета!

Я помогу вам с:
📅 Расписанием занятий
🎯 Мероприятиями
📝 Подачей заявлений
💡 И другой полезной информацией

Выберите действие ниже или откройте полное приложение:

[📅 Расписание] [🎯 Мероприятия] [📝 Заявления]
[🚀 Открыть приложение] ← НОВАЯ КНОПКА!
[❓ Помощь]
```

4. Нажмите **"🚀 Открыть приложение"**
5. Откроется полный веб-интерфейс с React чатботом

## 📝 Настройки в .env

```bash
BOT_TOKEN=your_max_bot_token
MINI_APP_URL=https://your-ngrok-url.ngrok.io  # или ваш домен
PORT=5000
```

## 🔧 Обновление после изменений

```bash
# Пересобрать и перезапустить
docker compose build chatbot
docker compose up -d

# Проверить логи
docker compose logs -f chatbot
```

## 📚 Подробнее

Смотрите полную документацию в `MINI_APP_SETUP.md`
