# 🎯 Что делать дальше

Все изменения для миграции на PostgreSQL и JWT аутентификацию **готовы**! 

## ✅ Что уже сделано

1. ✅ **Схема базы данных обновлена** на PostgreSQL
2. ✅ **JWT аутентификация реализована** (регистрация, вход, токены)
3. ✅ **Заявления привязаны к пользователям** с реальными именами
4. ✅ **Панель для преподавателей** - просмотр всех заявлений
5. ✅ **Бот автоматически создает пользователей** при первой подаче заявления
6. ✅ **Система ролей** (STUDENT, TEACHER, STAFF, ADMIN)
7. ✅ **Admin seeding** - автоматическое создание админа при запуске
8. ✅ **Документация** создана (4 файла)
9. ✅ **Зависимости установлены** (bcrypt, jsonwebtoken, pg)

## 📋 Следующие шаги (для вас)

### 1️⃣ Установите PostgreSQL

**Windows (Scoop рекомендуется):**
```powershell
scoop install postgresql
# или
choco install postgresql
```

**Или скачайте:** https://www.postgresql.org/download/windows/

### 2️⃣ Создайте базу данных

```powershell
# Запустите PostgreSQL (если не запущен)
pg_ctl start

# Создайте базу
psql -U postgres
```

В psql консоли:
```sql
CREATE DATABASE maxchatbot;
\q
```

### 3️⃣ Обновите server/.env

Откройте `server/.env` и проверьте/обновите:

```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/maxchatbot
```

Если пароль не установлен, используйте:
```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/maxchatbot
```

### 4️⃣ Примените миграцию

```powershell
cd server
npx prisma generate
npx prisma db push
```

### 5️⃣ Запустите сервер

```powershell
npm start
```

При первом запуске автоматически создастся admin из `.env`:
- Email: `admin@university.edu`
- Password: `changeme123`

### 6️⃣ Протестируйте API

```powershell
# Вход как admin
$body = @{
    email = "admin@university.edu"
    password = "changeme123"
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:5000/api/auth/login" `
    -Method POST -Body $body -ContentType "application/json"

Write-Host "✅ Login successful! Token: $($response.token.Substring(0,20))..."
```

Больше примеров в **`API_EXAMPLES.md`**

---

## 📚 Полезные файлы

| Файл | Описание |
|------|----------|
| **POSTGRES_QUICKSTART.md** | ⚡ Быстрая настройка за 5 минут |
| **MIGRATION_GUIDE.md** | 📖 Полная документация (API, безопасность, примеры) |
| **API_EXAMPLES.md** | 🧪 Примеры всех API запросов (PowerShell, Bash, Node.js) |
| **README_NEW.md** | 📘 Обновленный README с PostgreSQL и JWT |

---

## 🔧 Возможные проблемы

### PostgreSQL не запускается

```powershell
# Проверьте статус
pg_ctl status

# Запустите вручную
pg_ctl start -D "C:\Program Files\PostgreSQL\16\data"
```

### Порт 5000 занят

```powershell
# Найдите процесс
netstat -ano | findstr :5000

# Остановите
taskkill /PID <PID> /F

# Или измените порт в .env
PORT=5050
```

### Prisma ошибки

```powershell
# Очистите и пересоздайте
npx prisma generate
npx prisma db push --force-reset  # ⚠️ Удалит все данные!
```

---

## 🎯 Готово к использованию

После выполнения шагов выше у вас будет:

- ✅ PostgreSQL база данных
- ✅ JWT аутентификация
- ✅ Привязка заявлений к реальным пользователям
- ✅ Разграничение доступа по ролям
- ✅ Admin аккаунт готов к использованию
- ✅ Бот автоматически создает пользователей

**Следующий этап:** Обновить фронтенд (React) для работы с новыми API endpoints!

---

## 💡 Быстрые команды

```powershell
# Все в одном (после установки PostgreSQL и создания БД)
cd server
npx prisma generate; npx prisma db push; npm start

# В другом терминале: тест API
$token = (Invoke-RestMethod -Uri "http://localhost:5000/api/auth/login" -Method POST -Body '{"email":"admin@university.edu","password":"changeme123"}' -ContentType "application/json").token
Invoke-RestMethod -Uri "http://localhost:5000/api/auth/me" -Headers @{Authorization="Bearer $token"}
```

---

## 🚀 Production готовность

Перед деплоем обязательно:

1. ⚠️ Смените `JWT_SECRET` на случайный ключ
2. ⚠️ Смените пароль admin
3. ⚠️ Настройте HTTPS
4. ⚠️ Обновите CORS для вашего домена
5. ⚠️ Используйте переменные окружения для всех секретов
6. ⚠️ Настройте backup базы данных

---

**Удачи с настройкой! 🎉**

Если возникнут вопросы, смотрите документацию в файлах выше.
